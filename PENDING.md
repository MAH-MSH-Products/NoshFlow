# PENDING Tasks (Blockers & Waiting List)
This file tracks tasks that one developer is waiting for the other developer to complete in order to finish their phase.

## 🔴 Frontend (HajiAlirezaei) is waiting for:
**Current Status:** Phase 5 (Admin Panel - Menu Management) is complete and working with the real API. I can add and delete food items.

**Tasks the Backend (HamidiFard) needs to complete to finalize Phase 5:**
1. **[Phase 5 - API]:** Build a `GET /api/reports/sales` (or similar) endpoint that returns daily/weekly sales statistics so I can build the charts for the Admin Dashboard.

**Blocker Reason:** I cannot implement the Chart.js/Recharts visual reports until the backend provides the statistical data.

---

## 🟢 Backend (HamidiFard) is waiting for:
* *(Currently, the Backend is not waiting for the Frontend.)*